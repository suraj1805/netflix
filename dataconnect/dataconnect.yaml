specVersion: "v1beta"
serviceId: "netflix"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "netflix-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
